import * as c from './../actions/ActionTypes';

export default (state = null, action) => {
  switch (action.type) {
    case "SELECT_KEG": 
      return state[id].id
    default: return state; 
  }
  
}
